import React from 'react'
import classes from './release-date.module.css'

const ReleaseDate = () => (
  <div className={classes.releaseDate}>
    <span className={classes.lessSpacing}>'</span>Ou
    <span className={classes.lessSpacing}>t</span>' 26 January 2019
  </div>
)

export default ReleaseDate
